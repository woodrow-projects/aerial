# dependencies
node_modules/
.pnpm-store/

# build output
dist/
dist-bin/
build/
*.tsbuildinfo

# turbo
.turbo/

# test coverage
coverage/

# env / secrets — NEVER commit (see ADR D10)
.env
.env.*
!.env.example

# prisma
apps/control-plane/prisma/*.db

# runtime data (audio segments, media, logs)
/srv/
hls/
media/
# …but the control-plane media *module* source is code, not runtime data
!apps/control-plane/src/media/
# …and the web media-library *feature* source is code, not runtime data
!apps/web/src/features/media/
*.log

# editor / OS
.DS_Store
.vscode/
.idea/

# vite
apps/web/dist/
